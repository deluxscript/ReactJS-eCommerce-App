import React from 'react';

import SingleItem from './StoreItems/SingleItem';

const Mystore = ( props ) => {
    return (
        <div>
            <SingleItem />
        </div>
    );
}

export default Mystore;